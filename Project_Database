CREATE DATABASE school_management;
USE school_management;

CREATE TABLE users (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE,
    password VARCHAR(100),
    role ENUM('ADMIN','CLERK','TEACHER','PRINCIPAL'),
    name VARCHAR(100),
    email VARCHAR(100),
    phone VARCHAR(15)
);

CREATE TABLE students (
    student_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    parent_name VARCHAR(100),
    parent_phone VARCHAR(15),
    address TEXT,
    class VARCHAR(20),
    section VARCHAR(5),
    previous_performance VARCHAR(50),
    admission_date DATE
);

CREATE TABLE fees (
    fee_id INT AUTO_INCREMENT PRIMARY KEY,
    student_id INT,
    amount DECIMAL(10,2),
    payment_date DATE,
    payment_mode VARCHAR(20),
    status ENUM('PAID','PENDING'),
    FOREIGN KEY (student_id) REFERENCES students(student_id)
);

CREATE TABLE attendance (
    attendance_id INT AUTO_INCREMENT PRIMARY KEY,
    student_id INT,
    date DATE,
    status ENUM('PRESENT','ABSENT'),
    FOREIGN KEY (student_id) REFERENCES students(student_id)
);

CREATE TABLE performance (
    performance_id INT AUTO_INCREMENT PRIMARY KEY,
    student_id INT,
    subject VARCHAR(50),
    class_test_marks INT,
    exam_marks INT,
    grade VARCHAR(5),
    remarks TEXT,
    FOREIGN KEY (student_id) REFERENCES students(student_id)
);

CREATE TABLE syllabus (
    syllabus_id INT AUTO_INCREMENT PRIMARY KEY,
    class VARCHAR(20),
    subject VARCHAR(50),
    details TEXT,
    last_updated DATE
);

CREATE TABLE classrooms (
    room_no VARCHAR(10) PRIMARY KEY,
    capacity INT,
    building VARCHAR(50)
);

CREATE TABLE class_schedule (
    schedule_id INT AUTO_INCREMENT PRIMARY KEY,
    class VARCHAR(20),
    section VARCHAR(5),
    subject VARCHAR(50),
    teacher_id INT,
    room_no VARCHAR(10),
    schedule_time VARCHAR(50),
    FOREIGN KEY (teacher_id) REFERENCES users(user_id),
    FOREIGN KEY (room_no) REFERENCES classrooms(room_no)
);

SHOW TABLES;

INSERT INTO users (username, password, role, name)
VALUES ('admin', '1234', 'ADMIN', 'System Admin');

INSERT INTO students (name, parent_name, class, section)
VALUES ('Ravi Kumar', 'Suresh Kumar', '10', 'A');

SELECT * FROM users;
SELECT * FROM students;

delete from students
where student_id = 1;

INSERT INTO users (username, password, role, name)
VALUES 
('clerk1', '1234', 'CLERK', 'Ramesh'),
('teacher1', '1234', 'TEACHER', 'Suresh'),
('principal1', '1234', 'PRINCIPAL', 'Meena');

SELECT student_id, name FROM students;

SELECT * FROM fees;

SELECT * FROM attendance;

SELECT * FROM performance;

